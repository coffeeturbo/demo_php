feed:
  path: '/limit/{limit}/cursor/{cursor}'
  methods: [ GET ]
  defaults: { _controller: FeedBundle:Feed:feed }
  requirements:
    limit: \d+
    cursor: \d+
#    dateFrom: \d+
#    dateNow: \d+
#    profile: \d+
    order: "[a-z_]+"
    direction: ASC|DESC

profile_feed:
  path: '/profile/{profileId}/limit/{limit}/offset/{offset}'
  methods: [ GET ]
  defaults: { _controller: FeedBundle:ProfileFeed:feed }
  requirements:
    profileId: \d+
    limit: \d+
    offset: \d+
