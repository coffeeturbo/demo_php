feed(
  *ngIf="feed.length > 0", 
  [feed]="feed", 
  [isLoading]="isLoading", 
  [showRefresh]="true", 
  [isFeedEnd]="isFeedEnd", 
  [restoreScroll]="routeHelper.popState?.pop || false",
  (onFeedLoad) = "loadFeed($event)",
  (onRefresh)="refresh()"
)
post-not-found(*ngIf="feed.length == 0")
router-outlet