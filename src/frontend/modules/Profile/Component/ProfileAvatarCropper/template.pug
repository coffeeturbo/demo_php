doctype html
modal(width="500px", *ngIf="helper.ready()", [backdrop-close]="false", (on-close)="helper.destroy()", [animation]="false")
  loader(*ngIf="disabled")
  modal-header Загрузка изображения
  modal-body
    article
      p Выбрите область для маленьких фотографий.
      p Выбранная миниатюра будет использоваться в новостях, личных сообщениях и комментариях.
    figure
      .cropper-modal-box
        img(
          cropper,
          [src]="helper.src",
          [options]="cropperOptions",
          (onChange)="data = $event", 
        )
      .preview-area
        .preview.medium
        .preview.small
  modal-footer
    button.btn((click)="helper.destroy()") {{ "Cancel" | translate }}
    button.btn.primary((click)="submit()") {{ "Upload" | translate }}